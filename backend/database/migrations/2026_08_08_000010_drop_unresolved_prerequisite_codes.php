<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;

/**
 * حذف مراجع المتطلبات التي لا تقابل أي مساق.
 *
 * ستة مراجع بخمسة رموز (`EEE1 3250`, `EEE1 3251`, `EEE0 3150`,
 * `EEE1 3254`, `ACD0 3265`) وُلدت من كون المصدر صفحات عرض لا خطة
 * معتمدة: صفحة السنة نفسها تكتب إلكترونيات `EEE1 3259` ومتطلبها
 * `EEE1 3250` بينما دوائر كهربائية `EEE1 3258` — الرمز في المصدر
 * لا وجود له أصلًا.
 *
 * كانت تُعرض للطالب نصًّا كما وردت (قرار «لا يجوز التخمين في بيانات
 * أكاديمية»)، وقرّر المالك حذفها: رمزٌ لا يقابل مساقًا لا يُبحث به
 * ولا يُقرأ، فهو ضجيج في خانةٍ صارت تعرض الأسماء.
 *
 * والحذف مقصور على `prerequisite_course_id IS NULL` — أي غير المربوط
 * وحده. الـ٣١ المربوطة لا تُمسّ.
 *
 * والأعمدة تبقى كما هي: `prerequisite_code_raw` و`needs_review` ليستا
 * علاجًا لهذه الحالة بل للحالة الدائمة — مساق يُضاف بمتطلب لم يُنشأ
 * بعد، أو رمز يتغيّر في خطة قادمة. حذفهما يعني هجرة جديدة عند أول
 * تعديل في الخطة.
 */
return new class extends Migration
{
    public function up(): void
    {
        DB::table('course_prerequisites')
            ->whereNull('prerequisite_course_id')
            ->delete();
    }

    /*
     * لا تراجع: الصفوف المحذوفة رموز نصّية لا مفاتيح، ومصدرها
     * (`courses.json` وصفحات السنوات) حُذفت منه في الدفعة نفسها —
     * فإعادة إنشائها هنا تُحيي بيانات لم يعد لها أصل. من أراد
     * استرجاعها فمن تاريخ git لا من هجرة عكسية.
     */
    public function down(): void
    {
        //
    }
};
