-- خطوة إضافة "إضافة/تعديل/حذف محاضرة من داخل البوت" — عمود جديد يخزّن
-- حالة المحادثة متعددة الخطوات لكل طالب أثناء بنائه لمحاضرة جديدة أو
-- تعديل/حذف واحدة موجودة.
-- شغّل هذا السطر مرة واحدة فقط بـphpMyAdmin على قاعدة البيانات الحقيقية.

ALTER TABLE telegram_links
    ADD COLUMN pending_action TEXT NULL AFTER reminders_enabled;
